libraries是所用到的第三方库，有些库是安装到系统中的。故这里只放对应版本压缩包备份
common是数据服、逻辑服、DB引擎、LOG引擎、NET引擎共用的代码，如数据类型
DBEngine是LOG引擎，负责写玩家操作记录、消费产出（DB、NET的log比较少，不会通过LOG引擎去写）。
build是各个引擎生成的可运行文件拷贝存放，用于发布
libs是common中生成的各个.o文件存放处
doc是相关开发文档存放处
